#include<iostream>
using namespace std;
int main() {

    for (int = 1; i <= 5; i++)
    {

        for (int x = 0; x < 5 - i; x++) cout << " ";
        for (int x = 0; x < i * 2 - 1; x++) cout << "*";
        

    }
    return 0;
}
